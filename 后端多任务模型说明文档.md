# 后端多任务模型说明文档

**English project title:** *A WeChat Plugin Prototype for Fine-Grained User Psychological Risk Detection and Lightweight Response*

**中文概要：** 本项目为微信式插件原型，包含 **细粒度心理风险与状态检测**（多任务 S1/S2/S3 + 策略与风险评分）与 **轻量应答**（可配置外部 LLM、对话与 Admin）。**本文档**专门说明后端 **BERT 多任务检测模型**、弱监督规则、训练/评测与推理输出；**端到端运行、SQLite、WebSocket、LLM 配置、前端 mock** 等见根目录 **`README.md`**。

---

# Backend Multi-Task Model (S1 / S2 / S3 + BERT Multi-Head + LoRA)

This document explains the **detection** backend: task definitions, weak-supervision labeling rules (see code for full keyword lists), training/evaluation workflow, and inference output. It does **not** replace the LLM “response” path (`chat_llm.py`, persona prompts in code).

## 1. Task Definition (Three-Layer Task Design)

The model uses **multi-task learning** with one shared BERT encoder and three task-specific heads:

1. **Task 1: S1 (EFA 烦恼类型 1.1–1.19, single-label)**  
   - Type: **single-label** → `CrossEntropyLoss`  
   - **19** classes (`S1_LABELS` in `multitask_config.py`, ids `efa_s1_1_1_academic` … `efa_s1_1_19_parent_child`).

2. **Task 2: S2 (EFA 心理疾病 2.1–2.8)**  
   - Type: single-label  
   - **8** classes (抑郁/焦虑/躁郁/PTSD/恐慌/进食障碍/尚未达到S2/其它).

3. **Task 3: S3 (EFA SOS 3.1–3.6)**  
   - Type: single-label  
   - **6** classes: 正在自杀行为、策划自杀、自残、对他人正在伤害、计划对他人伤害、无。  
   - **Policy:** predicted class indices **0–4 (3.1–3.5)** trigger **mandatory `handoff_to_human`** in `multitask_predict.py` (in addition to `risk_scoring` + `policy_engine` for other cases).

## 2. Final Label Structure (Model Output for UI)

The raw model output is:

```json
{
  "text": "...",
  "s1": 12,
  "s2": 1,
  "s3": 3
}
```

The frontend maps it to display fields (example):

```json
{
  "问题类型": ["学业压力", "人际"],
  "心理状态": "焦虑",
  "风险等级": "中风险"
}
```

**Note:** **`风险等级`** is **`高风险`** whenever the predicted S3 class is **3.1–3.5** (forced handoff). Otherwise it follows **`risk_scoring.compute_risk_score`** (six-class S3 softmax + rules + optional history). **`action`** is **`handoff_to_human`** for S3 3.1–3.5, else from **`policy_engine.decide_policy(risk)`**. See `multitask_predict.py`.

## 3. Automatic Labeling from Existing Data (Weak Supervision)

The project can build training rows from the Weibo sentiment CSV:

`dataset/weibo/微博评论情感数据集(清洗之后的，有标注，中文,csv格式)/clean_weibo_text.csv`

CSV format: `label,text`  
`label` follows the dataset README:

- `0`: joy  
- `1`: anger  
- `2`: disgust  
- `3`: sadness  

S1/S2/S3 labels are generated by rules in **`multitask_config.map_s1` / `map_s2` / `map_s3`**. The code also matches a **whitespace-stripped** form of the text (e.g. `作 业` → `作业`) so keywords are not missed.

### 3.1 S2 Mapping (Weibo sentiment → mental state)

Rule (`map_s2()`):

- if `label == 3` (低落) → `depression`  
- if `label == 1` (愤怒) → `anxiety`  
- otherwise → `none`

### 3.2 S1 Mapping (keyword → one EFA-like class, weak supervision)

Rule (`map_s1` / `map_s1_weak_single`): returns a **single** S1 index for Weibo-style text (coarse mapping into 19 classes).  

**Authoritative keywords:** **`multitask_config.py`**.

### 3.3 S3 Mapping (risk keywords → EFA S3 index, weak supervision)

Rule (`map_s3` / `map_s3_weak`): maps phrases to **six** EFA-aligned indices (e.g. ideation vs self-harm vs harm-others).  

**Authoritative list:** **`multitask_config.map_s3_weak`**.

### 3.4 EFA JSON corpus → CSV

With a valid **`EFAQA_DL_LICENSE`**, run **`export_efaqa_to_csv.py`** to produce `text, s1, s2, s3` (integers) for **`train_multitask_lora.py`**.

## 4. Model Structure (One BERT + Three Heads)

Implementation file: `multitask_model.py`

Architecture:

- Backbone: `BERT` (Chinese checkpoint chosen in training script)  
- Heads:
  - `s1_head`: **19**-dim logits（EFA S1 单标签）  
  - `s2_head`: **8**-dim logits（EFA S2 单标签）  
  - `s3_head`: **6**-dim logits（EFA S3 单标签）  

Loss:

- S1 / S2 / S3: `CrossEntropyLoss`（S1 可用 **`--use_s1_pos_weight`** 启用 **CE 类权重**，参数名历史遗留）  
- S2 / S3 可选类权重：`--use_s2_class_weight` / `--use_s3_class_weight`  
- 多任务加权和（默认见 **`train_multitask_lora.py`**，如 `s1=1.0`, `s2=2.0`, `s3=5.0`）

## 5. LoRA Integration

Implementation file: `train_multitask_lora.py`

Typical LoRA setup (see script for current flags):

- `r=8`  
- `lora_alpha=16`  
- `target_modules=["query","value"]`  
- `task_type=SEQ_CLS`  

Trainable parameters are mainly LoRA adapters + classification heads (`s1_head/s2_head/s3_head`)。

**合并 checkpoint：** 训练结束时会将 **原始骨干 `converted_bert_pytorch` + 三头权重（从 `best_full_base_with_heads` 中读取）+ LoRA adapter** 合并为 `best_model/`。不要对 `best_full_base_with_heads` 单独做 `from_pretrained` 再合并，否则 BERT 权重键名与标准结构不一致，会导致评估指标异常。手动合并请用已修复的 `merge_peft_to_best_model.py`（与 `train_multitask_lora.py` 末尾逻辑一致）。

Dependency note: LoRA training requires `peft` (see `environment.yml` / `requirements-pip.txt`).

## 6. Key Backend-Related Files

- `multitask_config.py`: label definitions, weak-labeling rules, output formatting  
- `multitask_model.py`: multi-head task model  
- `multitask_data.py`: CSV → tensors (Weibo weak labels or explicit multitask columns)  
- `train_multitask_lora.py`: training pipeline  
- `multitask_predict.py`: inference (library + CLI)  
- `risk_scoring.py`, `policy_engine.py`: risk score and routing  
- `backend_api.py`: FastAPI + persistence + WebSocket (calls predict + LLM for chat)  
- `gradio.py` / `app_gradio.py`: model UI and backend monitor tabs  

## 7. Workflow (Training / Evaluation / Inference)

### 7.1 Before training

Install environment (recommended via `environment.yml`). Core dependencies include:

- `transformers`  
- `datasets`  
- `scikit-learn`  
- `peft`  
- `torch` (CUDA supported)  

If your network is restricted, use offline/local package installation.

### 7.2 Smoke test (small sample first)

Run in project root:

```powershell
python .\train_multitask_lora.py --max_samples 200 --epochs 1 --batch_size 4 --max_length 32 --output_dir multitask_output_test
```

### 7.3 Full training + save best model

**微博弱标注示例：**

```powershell
python .\train_multitask_lora.py --device cuda --epochs 8 --batch_size 16 --max_length 128 --output_dir multitask_output_v3
```

**EFA 导出 CSV（`text,s1,s2,s3`）示例：**

```powershell
python .\train_multitask_lora.py --weibo_csv_path .\dataset_efaqa_multitask.csv --epochs 15 --output_dir multitask_output
```

（`--weibo_csv_path` 名称沿用历史，实为任意支持 `explicit_s123` 或 Weibo 格式的 CSV。）

若需与 `eval_only.py` **复现训练时的 val/test 划分**，训练时增加：

```powershell
python .\train_multitask_lora.py ... --save_split_indices
```

会生成 `{output_dir}/split_indices.npz`（含 `train` / `val` / `test` 在原 CSV 行序上的下标）。

训练结束后目录中常见产物：

- `best_model/` — **已合并 LoRA** 的完整权重，供推理与 `eval_only.py`  
- `best_peft_adapter/` — 仅 LoRA  
- `best_full_base_with_heads/` — 训练中途快照（含 LoRA 键名；**勿**单独当标准 BERT 加载）  
- `metrics_report.json` — 验证/测试指标摘要  
- `converted_bert_pytorch/` — 若从 TF checkpoint 转换，会缓存 PyTorch 骨干  

### 7.4 Test-set evaluation（训练脚本内）

训练脚本在末尾对已合并的 `best_model` 再跑一轮 Val/Test，并写入 `metrics_report.json`。

### 7.4b 独立评估 `eval_only.py`

默认（在项目根目录运行、且存在 `dataset_efaqa_multitask.csv` 时）：

- 模型：`multitask_output/best_model`  
- 数据：优先 `dataset_efaqa_multitask.csv`，否则微博 CSV  

```powershell
python .\eval_only.py --device cuda
```

指定模型与数据、并导出混淆矩阵图与 JSON：

```powershell
python .\eval_only.py --model_dir .\multitask_output\best_model --weibo_csv_path .\dataset_efaqa_multitask.csv --plots_dir .\eval_plots --save_json .\eval_report.json
```

与训练 **完全一致** 的 val/test 子集（需同一 CSV、`--seed`、`--val_ratio`、`--test_ratio`，且未改 `--max_samples`）：

```powershell
python .\eval_only.py --split_npz .\multitask_output\split_indices.npz --weibo_csv_path .\dataset_efaqa_multitask.csv
```

### 7.5 Single-text inference

```powershell
python .\multitask_predict.py --model_dir .\multitask_output\best_model --text "我压力好大，作业又好多，朋友也不想理我"
```

（`multitask_predict.py` 默认 `model_dir` 亦为 `multitask_output/best_model`。）

The output is the structured format used by the backend / UI (plus internal fields such as `risk_score`, `action` when using `predict_text`).

### 7.6 Frontend demo (Gradio)

```powershell
python .\gradio.py
```

Enter text in the page to get structured prediction output.

## 8. Performance Metrics

Training/evaluation scripts compute metrics on `val/test` for all three tasks.

### 8.1 S1 (single-label, 19-class)

- `s1_accuracy`  
- `s1_precision_macro / s1_recall_macro`  

### 8.2 S2 (single-label)

- `s2_accuracy`  
- `s2_precision_macro`  
- `s2_recall_macro`  

### 8.3 S3 (single-label)

- `s3_accuracy`  
- `s3_precision_macro`  
- `s3_recall_macro`  

For risk modeling analysis, **S3 macro precision/recall** is usually the key focus because it better reflects **class imbalance** behavior.

## 9. Notes and Practical Considerations

Because S1/S3 training labels are partly built from **weak supervision** (keyword / sentiment rules):

- labels are **noisy**;  
- class distribution may be **imbalanced**;  
- metric fluctuations are expected.  

For thesis/defense wording, a concise contribution line is:

**“Fine-grained multi-task detection (S1–S2–S3) plus a lightweight LLM-based response path, in a WeChat-style prototype.”**  
（细粒度多任务心理信号检测 + 轻量应答，在类微信插件原型中落地。）

## 10. Relation to the Full Prototype

| Piece | Role in the project title |
|--------|----------------------------|
| S1 / S2 / S3 + policy | **Fine-grained** user psychological **risk** (and context) **detection** |
| External LLM chat + persona in `chat_llm.py` | **Lightweight response** (not the same as the BERT classifier) |
| `wechat_mock.html` / Admin | **WeChat plugin** UX **prototype** |

End-to-end behavior is documented in **`README.md`**.

## 11. 分层架构、risk_score 与误判说明

### 11.1 整体流水线（从用户输入到是否转人工）

1. **输入文本**（聊天正文，或 `wechat_mock` 发来的 `[moments] …` / `[status] …`；后端原样入库，**推理时会去掉方括号前缀**，只对正文做 BERT，减轻分布偏移，见 `multitask_predict._strip_bracket_event_prefix`）。  
2. **检测模型** `MultiTaskBertForPsychology`：共享 BERT 编码 + 三个线性头，输出 **S1/S2/S3** 的 logits → **S1/S2/S3 各自 argmax** 得到离散标签；S3 另做 **softmax** 得到六类概率。  
3. **连续风险分** `risk_scoring.compute_risk_score`：用 S3 的六类概率做 **加权和**（高危类权重大），再加 **关键词规则加分**（如自杀/自残/伤人短语），减去与「无风险」类相关的阻尼；若调用方传入 `history_scores` 还会做 **指数滑动与 sigmoid 融合**（当前 **`backend_api` 聊天路径未传历史**，故一般只有当前句）。  
4. **离散档位**：`score ≥ 0.60` → `高风险`，`0.30–0.60` → `中风险`，否则 `低风险`。  
5. **策略** `policy_engine.decide_policy`：低 → 仅 AI；中 → AI + 热线资源；高 → `handoff_to_human`。  
6. **额外硬规则**：`multitask_predict` 中若 S3 落在 **3.1–3.5**（索引 0–4）且 **该类 softmax 概率 ≥ 阈值**（默认与第 4 步「高风险」一致：**0.60**，即 `risk_scoring.RISK_LEVEL_HIGH_THRESHOLD`；可用环境变量 `MULTITASK_S3_HANDOFF_MIN_PROB` 或 CLI `--s3_handoff_min_prob` 单独调低以偏召回，例如 `0.42`），则 **无视档位直接强制转人工**；否则仅按第 4–5 步。  
7. **关键词兜底** `backend_api._apply_keyword_risk_guard`：句中含「自杀、轻生、想死…」等再强制转人工（防模型漏检）。

**展示给前端的** `风险等级`：强制 S3 成功时为「高风险」；否则为 `risk.level`。推理 JSON 中带 **`s3_pred_prob`、`s3_force_handoff`、`_risk_reasons`、`s3_handoff_min_prob`、`_text_used_for_model`**，便于 **Show Trace / inference_logs** 排查。

### 11.2 为何会出现「明显不该高危却转人工」

常见原因包括：

- **S3 误分类**：模型在域外文本（代码吐槽、英文状态、极短句）上 **argmax 落到 3.1–3.5**，在引入 **最小概率阈值** 前会一律转人工；阈值可抑制「低置信度的错误 argmax」。  
- **risk_score 过高**：即使 S3 argmax 为「无」，若 **多个高危类概率之和** 仍高（校准差、类别极不平衡），`base` 仍可能 ≥ 0.6 → 高风险 → 转人工。  
- **上下文混入**：若将来接入 `history_scores`，**历史高分**会把当前句「拖高」，短句单独看很安全也可能被判高（当前默认聊天 API 未传历史）。  
- **规则命中**：极少情况下用户字面包含兜底关键词会强制转人工。

改进方向：提高数据覆盖与校准、按需调整 `MULTITASK_S3_HANDOFF_MIN_PROB`（默认已与高风险分阈值对齐）、适度调整 `risk_scoring` 中档位阈值常量、对朋友圈/状态单独策略等。

### 11.3 训练过程可视化

- **Loss + 验证曲线**：`training_log.jsonl` 每行含 `epoch`、`train_loss_mean`、`train_loss_s1/2/3_mean`（新版本）、`val_metrics`、`composite`。绘图：  
  `python plot_training_log.py --log multitask_output/training_log.jsonl --out training_curves.png`  
- **EFA CSV 分布（EDA）**：`python visualize_efaqa_dataset.py --csv dataset_efaqa_multitask.csv --out efaqa_eda.png`  
- **混淆矩阵 / 评估报告**：`eval_only.py --plots_dir … --save_json …`（§7.4b）。

### 11.4 EFA 官方 label.s1/s2/s3 与「严重程度」、risk_score

- 语义上三个维度可以理解为：**S1 烦恼 → S2 是否达到疑似心理疾病层面 → S3 是否 SOS 紧急**；数字上 **1.x / 2.x / 3.x 量级递增**，但 **三者是三个独立分类头**，不是把 S1 当「低风险分」、S3 当「高分」的单一标量。  
- **`compute_risk_score` 的主体 `base` 只使用 **S3 的六类 softmax 概率** 加权（3.1–3.5 权重大，3.6 参与阻尼），**不直接读 S1/S2 的预测标签**。因此「S1/S2/S3 是否与 risk 算反了」：若指 **训练标签**，本仓库与官方 JSON 示例一致（s1=烦恼，s3=SOS）；若指 **risk 公式**，risk **故意只基于 SOS（S3）**，不用 S1/S2。S1/S2 主要驱动 **前端展示与 LLM 上下文**，误响应需同时查 **S3 误分 / 规则 boost / 强制转人工阈值**。
