# 后端多任务模型说明文档（S1/S2/S3 + BERT 多头 + LoRA）

本文档用于说明本项目后端“心理建模/风险检测”模型的实现方案、数据自动打标规则、训练与评估流程，以及推理接口输出格式。

## 1. 任务定义（对应论文三层任务）

本模型采用 **Multi-task learning（三任务联合学习）**，共享一个 BERT 编码器，分别输出：

1. **Task 1：S1（烦恼/问题类型）**  
   - 类型：多标签（multi-label）  
   - 标签集合（8类，简化自19类，可训练）：
     - `academic`（学业）
     - `career`（工作）
     - `family`（家庭）
     - `relationship`（情感）
     - `social`（人际）
     - `self`（自我/低自尊）
     - `stress`（压力）
     - `other`（其他）

2. **Task 2：S2（心理状态）**  
   - 类型：单标签（single-label）  
   - 标签集合（6类，作为论文亮点“心理分级建模”的核心）：
     - `depression`（抑郁）
     - `anxiety`（焦虑）
     - `bipolar`（双相）
     - `ptsd`（PTSD）
     - `panic`（恐慌）
     - `none`（无明显心理状态）

3. **Task 3：S3（风险检测）**  
   - 类型：单标签（single-label）  
   - 标签集合（答辩重点）：
     - `suicide_action`（自杀行为）
     - `suicide_ideation`（自杀意念）
     - `self_harm`（自伤）
     - `harm_others`（伤害他人）
     - `none`（无风险）

## 2. 最终标签结构（模型输出给前端/展示）

模型输出结构为：

```json
{
  "text": "...",
  "s1": [0,1,0,0,0,0,1,0],
  "s2": 1,
  "s3": 0
}
```

同时前端展示会把类别映射为中文可读形式：

```json
{
  "问题类型": ["学业压力", "人际"],
  "心理状态": "焦虑",
  "风险等级": "中风险"
}
```

## 3. 从现有数据“自动打标”（弱监督）

本项目目前主要使用仓库已有的中文微博情感数据：
`dataset/weibo/微博评论情感数据集(清洗之后的，有标注，中文,csv格式)/clean_weibo_text.csv`

该 CSV 格式为：`label,text`  
其中 `label` 来自微博情感极性标注，`README.md` 给出的含义为：
- `0`：喜悦
- `1`：愤怒
- `2`：厌恶
- `3`：低落

然后按规则自动生成 S1/S2/S3（弱监督）：

### 3.1 S2 映射（微博情感 -> 心理状态）

规则（对应你的给定 `map_s2()`）：
- 若 `label == 3`（低落） -> `depression`（抑郁）
- 若 `label == 1`（愤怒） -> `anxiety`（焦虑）
- 其它 -> `none`（无明显）

### 3.2 S1 映射（关键词 -> 问题类型，多标签）

规则（对应你的给定 `map_s1(text)`，当前版本为保留但简化）：
- 若文本包含 `作业` 或 `考试` -> 标注 `academic`
- 若文本包含 `朋友` -> 标注 `social`
- 若文本包含 `压力` -> 标注 `stress`

其它标签默认不触发（置0）。

### 3.3 S3 映射（风险关键词 -> 风险类别，当前版本实现关键类型）

规则（对应你的给定 `map_s3(text)` 的关键部分，已在代码中落地）：
- 若文本包含 `不想活` -> `suicide_ideation`
- 若文本包含 `割腕` -> `self_harm`
- 其它 -> `none`

说明：`suicide_action` 与 `harm_others` 当前未覆盖完整关键词规则；如你希望进一步增强 S3 五类表现，可以继续在 `multitask_config.py` 中扩展关键词映射。

## 4. 模型结构（一个 BERT + 多头输出）

实现文件：`multitask_model.py`

模型结构：
- Backbone：`BERT`
- Head：
  - `s1_head`: 输出 8 维（多标签 logits）
  - `s2_head`: 输出 6 维（单标签 logits）
  - `s3_head`: 输出 5 维（单标签 logits）

损失函数：
- S1（多标签）：`BCEWithLogitsLoss`
- S2（单标签）：`CrossEntropyLoss`
- S3（单标签）：`CrossEntropyLoss`
- 三者加权求和：`loss = loss_s1 + loss_s2 + loss_s3`（当前实现权重均为1）

## 5. LoRA 集成（在 BERT 上加适配器）

实现文件：`train_multitask_lora.py`

LoRA 配置要点：
- `r=8`
- `lora_alpha=16`
- `target_modules=["query","value"]`
- `task_type=SEQ_CLS`

训练时可训练参数主要来自 LoRA + 分类头（当前代码显式让 `s1_head/s2_head/s3_head` 可训练）。

依赖说明：训练 LoRA 需要 `peft` 包（仓库 `environment.yml` 已写入 `peft==0.11.1`）。

## 6. 需要的文件（你项目里有哪些与后端相关）

- `multitask_config.py`：标签定义与 S1/S2/S3 自动打标规则、前端展示格式化
- `multitask_model.py`：三任务多头模型实现
- `train_multitask_lora.py`：训练脚本（读 CSV -> 自动打标 -> 训练/验证/测试 -> 保存 best）
- `multitask_predict.py`：推理脚本（输入文本 -> 输出问题类型/心理状态/风险等级）
- `gradio.py`：Gradio 前端 demo（直接调用 `multitask_predict.py`）

## 7. 运行流程（训练 / 评估 / 推理）

### 7.1 训练前准备

确保你已经安装环境（建议使用仓库 `environment.yml` 创建 conda 环境），因为训练需要：
- `transformers`
- `datasets`
- `scikit-learn`
- `peft`（LoRA）
- `torch`（已包含 CUDA 支持）

如果你本地网络受限，务必使用本地/离线方式安装依赖包。

### 7.2 小样本 smoke test（建议先跑通链路）

在项目根目录运行：

```powershell
python .\train_multitask_lora.py --max_samples 200 --epochs 1 --batch_size 4 --max_length 32 --output_dir multitask_output_test
```

### 7.3 正式训练 + 保存 best 模型

```powershell
python .\train_multitask_lora.py --epochs 3 --batch_size 8 --max_length 128 --output_dir multitask_output
```

训练会在验证集上输出指标，并在训练结束后保存：
- `multitask_output/best_model/`

### 7.4 测试集评估

训练完成后脚本会打印 `test` 指标（基于 `best_model`）。

### 7.5 推理（单条文本）

```powershell
python .\multitask_predict.py --model_dir .\multitask_output\best_model --text "我压力好大，作业又好多，朋友也不想理我"
```

输出会是你前端展示需要的结构（中文映射后的“问题类型/心理状态/风险等级”）。

### 7.6 前端展示（Gradio）

```powershell
python .\gradio.py
```

在页面输入文本即可得到结构化输出。

## 8. 模型性能如何判断（指标说明）

训练脚本在 `val/test` 会分别计算三任务指标。

### 8.1 S1（多标签）指标

- `s1_subset_acc`：子集准确率（预测多标签集合必须与真实集合完全一致才算对）
- `s1_precision_micro / s1_recall_micro`：micro 精度/召回
- `s1_precision_macro / s1_recall_macro`：macro 精度/召回

### 8.2 S2（单标签）指标

- `s2_accuracy`
- `s2_precision_macro`
- `s2_recall_macro`

### 8.3 S3（单标签）指标（答辩重点）

- `s3_accuracy`
- `s3_precision_macro`
- `s3_recall_macro`

论文建议：重点在 `S3` 的 macro precision/recall（反映风险类别的识别能力，且对类别不均衡更敏感）。

## 9. 注意事项（弱监督与现实性）

由于当前 S1/S3 主要依赖关键词规则进行弱监督打标：
- 训练数据天然噪声较高；
- 类别分布可能不均衡；
- 因此指标会出现波动是正常的；
- 在答辩中建议把你的贡献点表述为：**“多任务联合建模 + 风险检测任务建模升级（S1-S2-S3）”**。

