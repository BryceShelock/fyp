# 项目数据与代码总览

**English version:** [`Project_Data_and_Code_Overview_EN.md`](Project_Data_and_Code_Overview_EN.md)

本文档汇总：**标签体系**、**当前仓库内 CSV 的类别占比（统计快照）**、**主要代码文件职责**及**相关原理**。统计基于 `multitask_config.py` 中的 EFA 对齐定义，在本地对现有 CSV 直接计数得到；**换数据或重新导出后需重新统计**。

---

## 1. 原理概览（你在实现什么）

| 模块 | 原理要点 |
|------|-----------|
| **多任务 BERT** | 共享编码器（Chinese BERT 等）+ **三个分类头**：S1、S2、S3 同时预测，联合损失训练。 |
| **S1** | **19 类单标签**（烦恼类型 1.1–1.19），交叉熵；可选类权重、采样。 |
| **S2** | **8 类单标签**（心理疾病维度 2.1–2.8，含「尚未达到心理疾病层面」），交叉熵。 |
| **S3** | **6 类单标签**（SOS 紧急程度 3.1–3.6），交叉熵；**连续风险分 `risk_score` 主要由 S3 的 softmax 加权得到**，再叠加规则短语与可选历史。 |
| **LoRA** | 低秩适配器微调 backbone，减小可训参数量；合并权重见 `merge_peft_to_best_model.py`。 |
| **推理策略** | `multitask_predict`：`risk_scoring` 算连续分与低/中/高档；若 S3 argmax 落在 3.1–3.5 且置信度 ≥ 阈值，可 **强制转人工**（与 `risk_scoring.RISK_LEVEL_HIGH_THRESHOLD` 默认对齐）。 |
| **产品策略** | `policy_engine.decide_policy` 按风险档给出 `action` 与资源位；`backend_api` 可再接 LLM、会话与人工队列。 |

---

## 2. 标签体系（全部类别与索引）

索引与 **`multitask_config.py`** 中列表顺序一致。EFA 原始标签字符串为 `1.x` / `2.x` / `3.x`，由 `efa_tag_to_*_index` 映射为整数。

### 2.1 S1：烦恼类型（19 类，`s1` ∈ 0..18）

| 索引 | EFA | 中文（展示） |
|------|-----|----------------|
| 0 | 1.1 | 学业/规划 |
| 1 | 1.2 | 事业与工作 |
| 2 | 1.3 | 家庭与矛盾 |
| 3 | 1.4 | 物质滥用与成瘾 |
| 4 | 1.5 | 悲恸 |
| 5 | 1.6 | 失眠 |
| 6 | 1.7 | 压力 |
| 7 | 1.8 | 人际关系 |
| 8 | 1.9 | 情感关系 |
| 9 | 1.10 | 离婚 |
| 10 | 1.11 | 分手 |
| 11 | 1.12 | 自我探索 |
| 12 | 1.13 | 低自尊 |
| 13 | 1.14 | 青春期问题 |
| 14 | 1.15 | 强迫症 |
| 15 | 1.16 | 其它烦恼 |
| 16 | 1.17 | LGBT+ |
| 17 | 1.18 | 性问题 |
| 18 | 1.19 | 亲子关系 |

### 2.2 S2：心理疾病疑似（8 类，`s2` ∈ 0..7）

| 索引 | EFA | 中文（展示） |
|------|-----|----------------|
| 0 | 2.1 | 抑郁（疑似） |
| 1 | 2.2 | 焦虑（疑似） |
| 2 | 2.3 | 躁郁（疑似） |
| 3 | 2.4 | 创伤后应激（疑似） |
| 4 | 2.5 | 恐慌（疑似） |
| 5 | 2.6 | 进食障碍（疑似） |
| 6 | 2.7 | 尚未达到心理疾病层面 |
| 7 | 2.8 | 其它心理疾病（疑似） |

### 2.3 S3：SOS / 身体伤害紧急度（6 类，`s3` ∈ 0..5）

| 索引 | EFA | 内部 id | 中文（展示） |
|------|-----|---------|----------------|
| 0 | 3.1 | efa_s3_suicide_action | 正在进行的自杀行为 |
| 1 | 3.2 | efa_s3_suicide_ideation | 策划/意念自杀 |
| 2 | 3.3 | efa_s3_self_harm | 自残 |
| 3 | 3.4 | efa_s3_harm_others_ongoing | 对他人正在人身伤害 |
| 4 | 3.5 | efa_s3_harm_others_planned | 计划对他人人身伤害 |
| 5 | 3.6 | efa_s3_none | 无紧急身体伤害倾向 |

**策略约定**：索引 **0–4**（3.1–3.5）在推理中可作为「强制转人工」的 S3 条件（见 `S3_FORCE_HANDOFF_INDICES`）。

---

## 3. 数据统计快照（当前仓库内 CSV）

以下行数与占比在 **2026-03-28** 对下列文件统计：**`dataset_efaqa_multitask.csv`**（20 000 行）、**`dataset_multitask.csv`**（400 000 行，经 `multitask_data.load_multitask_supervision` 解析）。

### 3.1 `dataset_efaqa_multitask.csv`（EFA 导出/对齐语料）

- **列**：`text, s1, s2, s3, md5`
- **行数**：20 000

**S3（关键）** — 极度不均衡：

| s3 | 中文 | 样本数 | 占比 |
|----|------|--------|------|
| 0 | 正在进行的自杀行为 | 0 | 0% |
| 1 | 策划/意念自杀 | 247 | 1.24% |
| 2 | 自残 | 88 | 0.44% |
| **3** | **对他人正在人身伤害** | **19 612** | **98.06%** |
| 4 | 计划对他人人身伤害 | 4 | 0.02% |
| 5 | 无紧急身体伤害倾向 | 49 | 0.25% |

**含义**：在此文件中，模型若拟合训练分布，会强烈偏向把 S3 预测为 **索引 3（3.4）**；这与日常「仅压力、烦恼」文本应接近 **3.6** 的期望严重冲突。**优先怀疑：语料线程级 `label.s3` 的真实语义、导出映射是否与官方定义一致，或该子集本身标注极度偏斜。** 不宜仅怪「后处理权重」。

**S2 摘要**：索引 6「尚未达到心理疾病层面」约 **89.0%**；其余类合计约 11%。

**S1 摘要**：索引 8「情感关系」约 **24.8%**；索引 15「其它烦恼」约 **35.8%**；其余类见 `visualize_efaqa_dataset.py` 出图或自行对 CSV `value_counts`。

### 3.2 `dataset_multitask.csv`（大规模混合表）

- **列**：`text, s1, s2, s3, source`（`s1` 等可为字符串形式，由 `multitask_data` 解析）
- **行数**：400 000（loader 格式 `explicit_s123`）

**S3（关键）** — 同样极度不均衡，且 **几乎全是索引 4**：

| s3 | 中文 | 样本数 | 占比 |
|----|------|--------|------|
| 0–3 | （多种） | 0 / 少量 | ≈0 |
| **4** | **计划对他人人身伤害** | **399 248** | **99.81%** |
| 5 | 无紧急身体伤害倾向 | 0 | 0% |

**含义**：该表 **不适合** 作为「均衡 S3 六分类」的主训练集；若曾用其训练 S3，模型会偏向预测 **3.5（索引 4）**。与 EFA 子集以 **3.4（索引 3）** 为主不同，**两文件主导的 S3 多数类不一致**，混训或换表会导致行为漂移。

**S1/S2 摘要**：S1 中「其它烦恼」约 **84.1%**；S2 中「尚未达到心理疾病层面」约 **88.2%**；多类计数为 0，说明该表标注空间覆盖很窄。

---

## 4. 其它数据与资源（路径级）

| 路径/文件 | 说明 |
|-----------|------|
| `export_efaqa_to_csv.py` | 从 `efaqa_corpus_zh.load()` 导出带 `label.s1/s2/s3` 的 CSV（需许可证环境变量）。 |
| `dataset/weibo/.../clean_weibo_text.csv` | 微博情感弱标注；`multitask_data` 可走 `text,label` 列映射弱标签。 |
| `dataset/chinese-chatbot-corpus/` | 第三方对话语料工程；`build_training_dataset.py` 可扫 `clean_chat_corpus/*.tsv` 拼弱标签 CSV。 |
| `build_training_dataset.py` | 合并聊天语料 + 微博等为多任务 CSV；S3 弱标签逻辑见 `multitask_config.map_s3_weak`（默认无关键词则 **3.6**）。 |
| `wechat_mock.html` / `admin.html` | 前端模拟；由 `backend_api` 同源挂载。 |

---

## 5. 代码文件功能索引（仓库根目录为主）

### 5.1 配置与数据

| 文件 | 功能与用途 |
|------|-------------|
| **`multitask_config.py`** | **单一真相源**：`S1/S2/S3_LABELS`、中英文映射、`efa_tag_to_*_index`、弱监督 `map_s1/map_s2/map_s3`、`format_prediction`。 |
| **`multitask_data.py`** | 读 CSV：`text,s1,s2,s3` 显式列或 `text,label` 弱标注；统一为训练用整数标签。 |

### 5.2 模型与训练

| 文件 | 功能与用途 |
|------|-------------|
| **`multitask_model.py`** | `MultiTaskBertForPsychology`：共享 BERT + 三线性头，前向返回 `s1/s2/s3_logits` 与联合 loss。 |
| **`train_multitask_lora.py`** | 主训练：DataLoader、**可选 S2/S3 类权重**、**S3 风险过采样 WeightedRandomSampler**、`s3_loss_weight`、验证指标、写 `training_log.jsonl`、保存 adapter 与三头权重。 |
| **`merge_peft_to_best_model.py`** | 将 LoRA 与 backbone/三头合并为可独立 `from_pretrained` 的 `best_model` 目录。 |
| **`eval_only.py`** | 仅评估：准确率、宏平均 P/R、混淆矩阵；可选保存图与 JSON。 |
| **`plot_training_log.py`** | 从 `training_log.jsonl` 画训练损失与验证曲线。 |
| **`visualize_efaqa_dataset.py`** | 对 EFA 风格 CSV 做 S1/S2/S3 与文本长度 EDA 图（不调用 `efaqa.load()`）。 |

### 5.3 推理、风险与策略

| 文件 | 功能与用途 |
|------|-------------|
| **`multitask_predict.py`** | **主推理入口**：tokenizer + 模型前向、S3 temperature softmax、**`risk_scoring`**、**强制转人工门控**、组装 JSON（含 `s3_probs_zh` 等）。 |
| **`risk_scoring.py`** | S3 六类 softmax **加权求和** + 规则短语加分 + `none` 微调 + 可选历史融合 → `risk_score` 与低/中/高档；**`RISK_LEVEL_HIGH_THRESHOLD=0.60`**。 |
| **`policy_engine.py`** | 由 `RiskResult` 映射到 `action`（如 AI 回复 / 带热线 / 转人工）与 `resources`。 |

### 5.4 服务与演示

| 文件 | 功能与用途 |
|------|-------------|
| **`backend_api.py`** | FastAPI：会话、WebSocket、管理端、**调用 `predict_text`**、静态页、可选 LLM（`chat_llm.py`）。 |
| **`chat_llm.py`** | 大模型调用封装（配置依赖环境与后端路由）。 |
| **`app_gradio.py`** | Gradio UI：选 `model_dir` / device，调用 `predict_text`。 |
| **`gradio.py`** | 薄入口，避免与包名 `gradio` 冲突，转调 `app_gradio.main()`。 |

### 5.5 数据构建与其它脚本

| 文件 | 功能与用途 |
|------|-------------|
| **`export_efaqa_to_csv.py`** | 官方 EFA 语料 → 本仓库 CSV 格式。 |
| **`build_training_dataset.py`** | 从聊天 TSV + 微博等构建弱监督多任务 CSV。 |
| **`download.py`** | 下载相关资源（视项目历史配置而定）。 |
| **`modle_train.py`** | 历史/备用训练脚本（文件名拼写保留）。 |
| **`wechat_data_simulate.py` / `data_simulate.py`** | 数据或会话模拟辅助。 |

### 5.6 文档

| 文件 | 功能与用途 |
|------|-------------|
| **`README.md`** | 快速上手、依赖与常用命令。 |
| **`后端多任务模型说明文档.md`** | API、风险与训练细节（与本文互补，偏「后端与模型说明」）。 |
| **`项目数据与代码总览.md`** | **本文**：标签全表、**占比快照**、代码地图与原理总览。 |

---

## 6. 如何自己复现「占比统计」

```bash
python visualize_efaqa_dataset.py --csv dataset_efaqa_multitask.csv --out efaqa_eda.png
```

或在 Python 中对 `dataset_efaqa_multitask.csv` 的 `s1,s2,s3` 列做 `value_counts` / `bincount`；对 **`dataset_multitask.csv`** 建议始终通过 **`load_multitask_supervision`** 解析后再统计，避免 `s1` 字符串格式误读。

---

## 7. 与你遇到的「总判 3.4 高风险」的直接关系

在当前 **`dataset_efaqa_multitask.csv` 快照** 中，**约 98% 的 `s3` 标签为索引 3（3.4 对他人正在人身伤害）**，**3.6 仅约 0.25%**。在此分布下，BERT **最小化交叉熵的最优策略之一就是大量输出类别 3**。这与「压力、写作业、好烦」等文本的常识标签不符，**根因是监督信号分布，而非 `risk_scoring` 单独算错**。改进路径：**核对 EFA 官方 `label.s3` 含义与导出脚本**、**补大量 3.6 与其它危机类均衡样本**、**调整训练权重与采样**，必要时 **重训 S3 头或全模型**。

---

## 8. 优化 `risk_score` 与模型（按优先级）

**原则**：数据与 S3 头校准是主战场；`risk_score` 与推理门控是「在现有 logits 上尽量少误报/少漏报」的二次塑形。

### 8.1 数据与标注（最高优先级）

- **核对** EFA 线程级 `label.s3` 与 `export_efaqa_to_csv.py` 映射是否与本项目 EFA 3.x 语义一致；当前快照里 **3.4 占 98%** 极不正常，需先确认是语料真分布还是导出/字段错误。
- **增补 `s3=5`（3.6 无紧急）** 的日常句、学业/人际烦恼句，使六类可学；目标至少让 **3.6 在训练中占可观比例**（例如 30%+ 视业务而定）。
- **避免** 把 `dataset_multitask.csv`（当前几乎全为 3.5）与 EFA 子集无策略混训，除非先统一或重标 S3。

### 8.2 训练侧（已在代码中支持或易改）

| 手段 | 作用 |
|------|------|
| `--s3_label_smoothing`（如 `0.05~0.1`） | 抑制 S3 softmax 过尖（**与 `--s3_focal_gamma` 同开时会忽略 smoothing**）。 |
| **`--s3_focal_gamma`**（如 `1.5~2`） | S3 改用 **Focal Loss**：易分样本权重 \((1-p_t)^\gamma\) 变小，减轻「多数类过置信」；可叠加 `--use_s3_class_weight`（**已加入 `train_multitask_lora.py`**）。 |
| 降低 `--s3_loss_weight`（如 `5→2`） | 减轻 S3 梯度支配，避免挤压 S1/S2 与表示空间。 |
| 关闭 `--use_weighted_sampler` | 在**已极度偏斜**的 S3 分布上再过采样危机类，可能加剧日常句误报。 |
| `--use_s3_class_weight` | 少数类极少时权重会极大，可与 smoothing、降权、增样本联合调。 |
| **`--use_s3_class_balanced_sampler`** | 训练按 **S3 六类逆频** 采样，少数类每 epoch 期望出现次数拉近，**宏 F1 常升、整体 accuracy 常降**（`train_multitask_lora.py`）。 |
| **`--best_select s3_macro_f1`** 或 **`macro_f1_sum`** | 存 best 时不再只看 `accuracy` 之和，而看 **验证集 S3 宏 F1** 或 **三任务宏 F1 之和**，避免「全猜多数类」当选。 |
| 验证集上选 `MULTITASK_S3_SOFTMAX_TEMPERATURE` | 推理温度与训练后校准常一起搜。 |

### 8.3 `risk_score` 与推理门控（已在代码中支持）

| 环境变量 | 作用 |
|----------|------|
| **`MULTITASK_RISK_UNIFORM_BLEND`**（如 `0.12~0.2`） | 对进入 `risk_scoring` 的 S3 概率做 `p'=(1-ε)p+ε/6`，压低错误 argmax=1 时的 `model_base`（**默认 0**，**已接入 `risk_scoring.py`**）。 |
| **`MULTITASK_RISK_NONE_DAMP_SCALE`**（默认 `0.10`） | 放大「`p_none` 对总分的下调」；可略增（如 `0.15~0.25`）以在 `p_none` 被低估时多留一点缓冲（**已接入**）。 |
| `MULTITASK_S3_SOFTMAX_TEMPERATURE` | 在 `multitask_predict` 里先软化 S3 logits，再 softmax（与 blend 可叠用，建议网格试）。 |
| `MULTITASK_S3_HANDOFF_MIN_PROB` | 强制转人工的置信度下限（默认与高风险分阈值 `0.60` 对齐）。 |

**注意**：blend / 温度只能缓解症状；**真正稳定的 3.6 与危机类区分仍依赖标签与训练**。

### 8.4 进阶（可选）

- 验证集上做 **temperature scaling** 或 **向量校准**，把校准后的概率再喂给 `risk_scoring`。
- 多任务 **联合置信**：例如仅当 S2 非「尚未达到心理疾病」且 S3 危机概率高时才升高风险（需产品定义，慎用漏报）。

---

## 9. 默认推理模型路径

仓库内统一使用 **`multitask_config.DEFAULT_INFERENCE_MODEL_DIR`**（当前为 `multitask_output_efa/best_model`）。正式训练请 **`--output_dir multitask_output_efa`**，使合并后的权重与后端 / Gradio / `eval_only` 默认一致；改目录名时只改该常量一处即可。

---

*文档中的占比数字随 CSV 变更而变；更新数据后请重新运行统计并修订 §3。*
